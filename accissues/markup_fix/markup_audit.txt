ACC markup audit — findings requiring a human decision
============================================================

Generated by 08_markup_fix.py against acc.txt.
Items below were DETECTED but NOT modified by the fixer.

acc.txt is structurally simpler than other Cologne dictionaries:
only 2 paired tag types (<symbol>, <F>) are used. The many
unpaired tags (<L>, <pc>, <k1>, <k2>, <LEND>, <HI1>, <HI>,
<H>, <P>, <e>) are record-delimiter structural tags.

------------------------------------------------------------

WHAT THIS FIXER AUTO-CORRECTS
------------------------------------------------------------
  - Nested <ab> (guard for future overlay passes — 0 now)
  - Whitespace inside <symbol> and <F>

Output goes to acc_fixed.txt (expected byte-identical to
source); change log in markup_fix_changes.txt.

------------------------------------------------------------

WHAT NEEDS HUMAN ATTENTION
------------------------------------------------------------
  1. acc.txt is currently clean on all standard markup checks.
     No whitespace hits, no nested tags, no boundary collisions.

  2. <F> tag (24 occurrences) — usage appears to be footnote
     markers. Visual review is cheap at this count:
       grep -n '<F>' acc.txt

  3. <symbol> tag (9,288 occurrences) — encodes special
     characters. Verify content is well-formed.

  4. 36 {{old -> new || …}} correction records exist.
     Any nested tags inside them are part of the format.

------------------------------------------------------------

AUTOMATED CHECKS BELOW
------------------------------------------------------------

## Adjacent </symbol> <symbol> — possibly intentional
   matches: 0 (showing up to 200)

## Adjacent </F> <F>
   matches: 0 (showing up to 200)

## Nested <ab> (guard — currently 0)
   matches: 0 (showing up to 200)

## Empty content <symbol> or <F> tag
   matches: 0 (showing up to 200)

## {#…#} closing brace immediately followed by <symbol> or <F>
   matches: 0 (showing up to 200)

## Malformed tag with unescaped < inside attribute value
   matches: 0 (showing up to 200)

